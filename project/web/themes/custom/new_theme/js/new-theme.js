/**
 * @file
 * New Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.newTheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
